import React from 'react';

// Simple placeholder component to fix build error.
// Replace with the real implementation of TranslationGame when ready.
export const TranslationGame: React.FC = () => {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold mb-2">Translation Game</h2>
      <p className="text-gray-600">
        Translation game coming soon.
      </p>
    </div>
  );
};

export default TranslationGame;
